# git-like
git-like version control system

## Зависимости

Для сборки проекта требуется **OpenSSL** (заголовочные файлы и библиотеки).

### Установка зависимостей

**Ubuntu/WSL:**
```bash
sudo apt update
sudo apt install libssl-dev